// constants/colorWheelConstants.js
// Re-exports from colorSchemes.js — single source of truth for all scheme data.
// Edit colorSchemes.js to add/change schemes; counts and offsets update automatically.
export { SCHEME_OFFSETS, SCHEME_COUNTS } from './colorSchemes';
