# Fashion Color Wheel Backend - Production Environment
# This file will be configured in Railway with actual values

# Database Configuration (Railway MySQL)
DB_HOST=${{MYSQL_HOST}}
DB_PORT=${{MYSQL_PORT}}
DB_NAME=${{MYSQL_DATABASE}}
DB_USER=${{MYSQL_USER}}
DB_PASSWORD=${{MYSQL_PASSWORD}}

# JWT Secret (generate a secure random string)
JWT_SECRET=${{JWT_SECRET}}

# Server Configuration
PORT=${{PORT}}
NODE_ENV=production

# CORS Configuration (add your app domains)
ALLOWED_ORIGINS=https://your-app-domain.com,https://fashioncolorwheel.app

# Rate Limiting (production settings)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# File Upload Configuration
MAX_FILE_SIZE=5242880
UPLOAD_PATH=./uploads
