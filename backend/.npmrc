# Railway/Nixpacks npm configuration
# Suppress deprecation warnings and use modern npm options

# Suppress the --production deprecation warning
audit=false
fund=false

# Use modern omit syntax instead of --production
omit=dev

# Performance optimizations for Railway builds
prefer-offline=true
no-audit=true
no-fund=true
progress=false
